name: rag-eval
channels:
  - conda-forge
  - pytorch
dependencies:
  - python=3.12
  - pip
  - numpy
  - pandas
  - pytorch
  - pip:
      - sentence-transformers>=2.6.0
      - transformers>=4.40.0
      - chromadb>=0.4.24
      - ragas>=0.1.8
      - datasets>=2.18.0
      - openai>=1.0.0
      - huggingface-hub>=0.23.0
      - pypdf>=4.0.0
      - python-docx>=1.1.0
      - python-dotenv
      - rich
      - tqdm
